{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Node",
            "type": "node",
            "request": "attach",
            "remoteRoot": "/usr/app/",
            "port": 9229,
            "address": "localhost",
            "localRoot": "${workspaceFolder}/",
            "protocol": "inspector",
            "restart": true
        }
    ]
}
